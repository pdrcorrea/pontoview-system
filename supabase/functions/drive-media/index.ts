import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { admin, cors, getDriveAccessToken, handleError, HttpError, requirePlayer } from "../_shared/common.ts";

Deno.serve(async (req) => { if (req.method === "OPTIONS") return new Response("ok", { headers: cors }); if (req.method !== "POST") return new Response("Method Not Allowed", { status: 405, headers: cors }); try { const manifest = await requirePlayer(req); const body = await req.json(); const mediaId = String(body.mediaId || ""); const { data: media } = await admin.from("media").select("id,organization_id,drive_file_id,drive_mime_type,drive_connection_id,status").eq("id", mediaId).eq("organization_id", String(manifest.organization.id)).single(); if (!media || !media.drive_file_id || !media.drive_connection_id || media.status !== "ready") throw new HttpError(404, "MEDIA_NOT_FOUND"); const token = await getDriveAccessToken(media.drive_connection_id); const response = await fetch(`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(media.drive_file_id)}?alt=media`, { headers: { Authorization: `Bearer ${token}` } }); if (!response.ok || !response.body) throw new HttpError(response.status === 404 ? 404 : 502, "DRIVE_DOWNLOAD_FAILED"); return new Response(response.body, { status: 200, headers: { ...cors, "Content-Type": response.headers.get("content-type") || media.drive_mime_type || "application/octet-stream", "Content-Length": response.headers.get("content-length") || "", "Cache-Control": "private, max-age=300", "X-Content-Type-Options": "nosniff" } }); } catch (error) { return handleError(error); } });
