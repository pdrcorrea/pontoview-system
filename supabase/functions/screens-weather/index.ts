import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { admin, cors, handleError, reply, requirePlayer } from "../_shared/common.ts";

const TTL=10*60*1000;const key=(lat:number,lon:number)=>`${lat.toFixed(4)},${lon.toFixed(4)}`;
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});if(req.method!=="POST")return reply({error:"METHOD_NOT_ALLOWED"},405);try{const manifest=await requirePlayer(req);const configured=manifest.settings?.weather_location||manifest.organization?.settings?.weatherLocation||null;if(!configured)return reply({temperature:null,name:"Configure a cidade"});const latitude=Number(configured.latitude);const longitude=Number(configured.longitude);const name=String(configured.name||"Clima");if(!Number.isFinite(latitude)||!Number.isFinite(longitude))return reply({temperature:null,name:"Cidade inválida"});const locationKey=key(latitude,longitude);const{data:cached}=await admin.from("weather_cache").select("temperature,apparent_temperature,humidity,wind_speed,weather_code,is_day,temp_min,temp_max,fetched_at").eq("location_key",locationKey).maybeSingle();if(cached?.fetched_at&&Date.now()-new Date(cached.fetched_at).getTime()<TTL)return reply({...cached,name,cached:true});const url=new URL("https://api.open-meteo.com/v1/forecast");url.searchParams.set("latitude",String(latitude));url.searchParams.set("longitude",String(longitude));url.searchParams.set("current","temperature_2m,relative_humidity_2m,apparent_temperature,is_day,weather_code,wind_speed_10m");url.searchParams.set("daily","temperature_2m_max,temperature_2m_min");url.searchParams.set("timezone","auto");url.searchParams.set("forecast_days","1");const response=await fetch(url);if(!response.ok){if(cached)return reply({...cached,name,cached:true,stale:true});return reply({error:"WEATHER_PROVIDER_ERROR"},502)}const json=await response.json();const row={location_key:locationKey,latitude,longitude,provider:"open_meteo",temperature:json.current?.temperature_2m??null,apparent_temperature:json.current?.apparent_temperature??null,humidity:json.current?.relative_humidity_2m??null,wind_speed:json.current?.wind_speed_10m??null,weather_code:json.current?.weather_code??null,is_day:json.current?.is_day===1,temp_min:json.daily?.temperature_2m_min?.[0]??null,temp_max:json.daily?.temperature_2m_max?.[0]??null,source_time:json.current?.time??null,fetched_at:new Date().toISOString(),updated_at:new Date().toISOString()};await admin.from("weather_cache").upsert(row);return reply({...row,name,cached:false})}catch(error){return handleError(error)}});
