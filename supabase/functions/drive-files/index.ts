import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { admin, cors, getDriveAccessToken, handleError, reply, requireOrgRole, requireUser } from "../_shared/common.ts";

Deno.serve(async (req) => { if (req.method === "OPTIONS") return new Response("ok", { headers: cors }); if (req.method !== "POST") return reply({ error: "METHOD_NOT_ALLOWED" }, 405); try { const user = await requireUser(req); const body = await req.json(); const { data: membership } = await admin.from("organization_users").select("organization_id").eq("user_id", user.id).order("created_at").limit(1).single(); const orgId = membership?.organization_id || ""; await requireOrgRole(user.id, orgId, ["owner", "admin", "editor"]); const { data: connection } = await admin.from("drive_connections").select("id").eq("organization_id", orgId).eq("status", "active").order("created_at", { ascending: false }).limit(1).maybeSingle(); if (!connection) return reply({ error: "DRIVE_NOT_CONNECTED" }, 409); const token = await getDriveAccessToken(connection.id); const folderId = String(body.folderId || "root").replace(/'/g, ""); const url = new URL("https://www.googleapis.com/drive/v3/files"); url.searchParams.set("q", `'${folderId}' in parents and trashed = false`); url.searchParams.set("pageSize", "100"); url.searchParams.set("orderBy", "folder,name"); url.searchParams.set("fields", "files(id,name,mimeType,thumbnailLink,modifiedTime,md5Checksum,size,videoMediaMetadata,parents),nextPageToken"); const response = await fetch(url, { headers: { Authorization: `Bearer ${token}` } }); const payload = await response.json(); if (!response.ok) return reply({ error: "DRIVE_LIST_FAILED" }, 502); const files = (payload.files || []).filter((file: any) => file.mimeType === "application/vnd.google-apps.folder" || file.mimeType?.startsWith("image/") || file.mimeType?.startsWith("video/")).map((file: any) => ({ ...file, connectionId: connection.id, isFolder: file.mimeType === "application/vnd.google-apps.folder" })); await admin.from("drive_connections").update({ last_sync_at: new Date().toISOString() }).eq("id", connection.id); return reply({ files, nextPageToken: payload.nextPageToken || null }); } catch (error) { return handleError(error); } });
