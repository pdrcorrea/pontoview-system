import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { cors, handleError, HttpError, reply, requireOrgRole, requireUser, signState } from "../_shared/common.ts";

Deno.serve(async (req) => { if (req.method === "OPTIONS") return new Response("ok", { headers: cors }); if (req.method !== "POST") return reply({ error: "METHOD_NOT_ALLOWED" }, 405); try { const user = await requireUser(req); const body = await req.json(); const organizationId = String(body.organizationId || ""); let orgId = organizationId; if (!orgId) { const { admin } = await import("../_shared/common.ts"); const { data } = await admin.from("organization_users").select("organization_id").eq("user_id", user.id).order("created_at").limit(1).single(); orgId = data?.organization_id || ""; } await requireOrgRole(user.id, orgId, ["owner", "admin"]); const appOrigin = Deno.env.get("APP_ORIGIN") || ""; const requestedReturn = String(body.returnTo || `${appOrigin}/conteudo`); const returnTo = requestedReturn.startsWith(appOrigin) ? requestedReturn : `${appOrigin}/conteudo`; const redirectUri = Deno.env.get("GOOGLE_REDIRECT_URI") || `${Deno.env.get("SUPABASE_URL")}/functions/v1/drive-oauth-callback`; const state = await signState({ userId: user.id, organizationId: orgId, returnTo, exp: Date.now() + 10 * 60_000 }); const url = new URL("https://accounts.google.com/o/oauth2/v2/auth"); url.searchParams.set("client_id", Deno.env.get("GOOGLE_CLIENT_ID") || ""); url.searchParams.set("redirect_uri", redirectUri); url.searchParams.set("response_type", "code"); url.searchParams.set("scope", "openid email https://www.googleapis.com/auth/drive.readonly"); url.searchParams.set("access_type", "offline"); url.searchParams.set("include_granted_scopes", "true"); url.searchParams.set("prompt", "consent select_account"); url.searchParams.set("state", state); if (!Deno.env.get("GOOGLE_CLIENT_ID")) throw new HttpError(503, "GOOGLE_OAUTH_NOT_CONFIGURED"); return reply({ url: url.toString() }); } catch (error) { return handleError(error); } });
